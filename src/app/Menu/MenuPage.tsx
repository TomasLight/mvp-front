import React, { FunctionComponent } from "react";

import { Typography } from "@material-ui/core";

const MenuPage: FunctionComponent = (props) => {
    return (
        <Typography>
            test test
        </Typography>
    );
};

export { MenuPage };
